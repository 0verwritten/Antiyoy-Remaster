{
  "name": "antiyoy-remaster-devtools",
  "private": true,
  "description": "Headless sim + browser smoke tests for the web remaster (run outside web/ to respect lakebed's no-install rule)",
  "type": "module",
  "scripts": {
    "sim": "tsx sim.ts",
    "browser": "node browser-check.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.50.0",
    "tsx": "^4.22.0"
  }
}
